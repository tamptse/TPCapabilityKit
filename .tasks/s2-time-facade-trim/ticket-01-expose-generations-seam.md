# 01: Expose generations time seam beside the facade

**What to build:** deterministic-time controls reachable through the narrower generations seam while the facade trio still exists, so migration batches have a green target before anything is deleted.

**Blocked by:** None (can start immediately as a work item; cite prune-stable trigger evidence in-ticket before editing code: prune-then-loop plus snapshot sum-vs-share green across the reconfigure-drain cycles with no fourth lock domain, plus same-file ordering vs the state-observe waiter work if unmerged).

**Status:** ready-for-agent

- [ ] Generations deterministic interface is crossable by tests (enable with collapsed fresh-instance plus precede-first-use gate beside the pruned count, advance, waiter rendezvous) while the facade trio remains untouched; both seams produce identical outcomes
- [ ] Time adapter stays a pure live/virtual adapter (enable-once, virtual-only gates unchanged); shared slot domain plus shared clock wiring preserved; Store-to-Tasks lock order and prune-then-loop discipline verbatim
- [ ] Production facade (configure, schedule, schedule-and-wait, cancel, pending, active) behavior unchanged; full suite green with zero behavior-semantics edits
